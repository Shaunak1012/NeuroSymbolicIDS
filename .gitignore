# ── Virtual environments ──
venv/
.venv/
env/
ENV/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ── Data (raw inputs + large generated intermediates) ──
data/raw_csv/
data/raw_pcaps/
data/processed/

# ── Trained models + fitted transformers ──
models/

# ── Large generated outputs (regenerable) ──
outputs/arrays/
outputs/embeddings/
outputs/predictions/
outputs/metadata/
outputs/*.log

# NOTE: outputs/figures/ is intentionally NOT ignored — commit eval plots.

# ── Copyrighted / reference material (do NOT publish) ──
basepaper.pdf
*.pdf

# ── OS / IDE ──
.DS_Store
Thumbs.db
.idea/
.vscode/
